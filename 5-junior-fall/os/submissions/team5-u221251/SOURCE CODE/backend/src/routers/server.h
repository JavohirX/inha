#ifndef SERVER_H
#define SERVER_H

/**
 * @brief Starts the server, listens for incoming client connections,
 *        and creates a dedicated thread for each connected client.
 */
void start_server();

#endif // SERVER_H
